# Maddy Marchesani Portfolio Website

Open `index.html` to preview.

To publish on GitHub Pages, upload the contents of this folder so `index.html`, `about.html`, `work.html`, `assets/`, and `collections/` are at the top level of the repository.

Notes:
- Homepage, My Work and Collections tiles are clickable.
- Tiles reveal titles on hover.
- Carousels use the images supplied in the uploaded ZIP folders.
- The Horizon ZIP contained only `.DS_Store` metadata and no usable image files, so the Horizon carousel is left as a placeholder.


Update included: My Work hover titles were strengthened and collection detail pages were rebuilt in the Horizon-style template. Horizon still needs its final image files added because the uploaded Horizon ZIP contained no usable images.


## Important preview note
Open `index.html` from the extracted folder first. All collection pages now sit at the top level of the folder so the image and style paths match the rest of the site. Do not open files from inside the ZIP preview; extract the ZIP first.
